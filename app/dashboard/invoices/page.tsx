import SideNav from "@/app/ui/dashboard/sidenav";

export default function Page(){
    // 编写一个表单，有用户名和密码输入项，使用 tailwindcss控制样式
    // 编写一个按钮，点击按钮后，使用 fetch api 向后端发送请求，请求地址为 /api/login
    // 编写一个表格，表格有三列，分别为：发票号、发票金额、发票日期
    // 使用 fetch api 向后端发送请求，请求地址为 /api/invoices

    return (<p>Invoices Page</p>)
}
